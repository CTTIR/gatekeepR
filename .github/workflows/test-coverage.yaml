on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: test-coverage

permissions: read-all

jobs:
  coverage:
    uses: CTTIR/.github/.github/workflows/test-coverage.yaml@main
    with:
      floor: 95
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
