% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{gk_prepare}
\alias{gk_prepare}
\title{Prepare marker signals for gating}
\usage{
gk_prepare(x, config, image_id = NULL, quiet = FALSE)
}
\arguments{
\item{x}{A \code{cellspec} object (see \code{\link[=gk_cellspec]{gk_cellspec()}}) or the path of a
cellspec directory.}

\item{config}{A \code{\link[=gk_config]{gk_config()}}.}

\item{image_id}{\code{NULL} for all images, or the images to keep.}

\item{quiet}{Suppress the summary message?}
}
\value{
An object of class \code{gk_prepared}, a list with:
\itemize{
\item \code{cells}: data frame \code{image_id}, \code{cell_id}, \code{sample_id}, \code{x}, \code{y};
\item \code{signal_raw}: cells x panel markers, selected raw signal (\code{NA} where
unavailable);
\item \code{score}: transformed signal;
\item \code{source}: integer matrix, 1 = preferred compartment, 2 = fallback,
0 = unavailable; \code{source_labels} describes the features;
\item \code{localization}: named list of per-marker data frames with
\code{numerator}, \code{denominator}, \code{ratio} and \code{evaluable};
\item \code{callability}: see \code{\link[=gk_callability]{gk_callability()}};
\item \code{transforms}: marker, transform and cofactor;
\item \code{config}, \code{config_sha256}, \code{source_sha256} and \code{source_manifest_sha256}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Selects one signal per cell and marker according to the configuration's
signal policy, applies the per-marker transform, resolves localisation
measurements, and computes callability checks that run before any
correction or threshold (see \code{\link[=gk_callability]{gk_callability()}}).

A panel marker whose measurement is missing from the cell table is a
structural failure (\code{gatekeepr_error_structure}). A marker that is present
but carries no usable signal is not an error: it is reported as not
callable, and every rule depending on it is disabled later.
}
\examples{
x <- gk_read_cellspec(gk_example_path("example-slide"))
prep <- gk_prepare(x, gk_example_config())
prep
gk_callability(prep)

}
\seealso{
Other preparation:
\code{\link[=gk_callability]{gk_callability()}}
}
\concept{preparation}
