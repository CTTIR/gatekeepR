% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config-hierarchy.R
\name{hierarchy-elements}
\alias{hierarchy-elements}
\alias{gk_rule}
\alias{gk_flag}
\alias{gk_refinement}
\alias{gk_override}
\title{Hierarchy rules, flags, refinements and overrides}
\usage{
gk_rule(
  rule_id,
  label,
  all_of = character(),
  any_of = character(),
  none_of = character(),
  on_uncallable = "disable_rule",
  review = NULL,
  pending_label = NULL,
  decisions = NULL
)

gk_flag(all_of = character(), any_of = character(), none_of = character())

gk_refinement(rule_id, marker, parent, from_label, to_label)

gk_override(
  image_id,
  marker,
  parent = NA_character_,
  threshold_method = NULL,
  threshold_args = NULL,
  reason
)
}
\arguments{
\item{rule_id}{Stable identifier, e.g. \code{"R010"}.}

\item{label}{Cell type assigned by the rule.}

\item{all_of, any_of, none_of}{Character vectors of marker names.}

\item{on_uncallable}{What to do with markers in \code{none_of} that are not
callable on an image: \code{"disable_rule"} (default) or
\code{"treat_as_negative"}. Markers in \code{all_of} or \code{any_of} that are not
callable always disable the rule.}

\item{review}{\code{NULL}, or \code{"structure"}: matching cells start as
\code{pending_label} until a structure decision exists (see
\code{\link[=gk_decide_structure]{gk_decide_structure()}}).}

\item{pending_label}{Label of cells awaiting a structure decision.}

\item{decisions}{Named list or character vector mapping each decision
(\code{Tumor}, \code{Benign}, \code{Unresolved}) to a label.}

\item{marker, parent}{State marker and parent set of a refinement or
override.}

\item{from_label, to_label}{Labels before and after refinement.}

\item{image_id}{Image the override applies to.}

\item{threshold_method, threshold_args}{Replacement method and arguments;
\code{NULL} keeps the panel's.}

\item{reason}{Why the override exists (required, recorded).}
}
\value{
Objects of class \code{gk_rule}, \code{gk_flag}, \code{gk_refinement} or
\code{gk_override}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Building blocks of a phenotype hierarchy:
\itemize{
\item \code{gk_rule()} assigns \code{label} to cells positive for every marker in
\code{all_of}, for at least one marker in \code{any_of} (ignored when empty) and
negative for every marker in \code{none_of}. Rules are evaluated in order and
the first match wins.
\item \code{gk_flag()} is a named Boolean expression evaluated before the rules;
its policy (\code{exclude}, \code{flag_only} or \code{unresolved}) is set in
\code{\link[=gk_hierarchy]{gk_hierarchy()}}.
\item \code{gk_refinement()} relabels cells of \code{from_label} that are positive for an
enabled state call (\code{marker} within \code{parent}), e.g. CD4 T cells that are
FOXP3-positive become Treg cells.
\item \code{gk_override()} replaces the threshold method or arguments of one marker
on one image, with a mandatory reason. Overrides are part of the
configuration and its hash; they never live in code.
}
}
\examples{
gk_rule("R020", "CD8 T cells", all_of = c("CD45", "CD3e", "CD8"), none_of = "CD4")
gk_rule(
  "R010", "Epithelial cells",
  all_of = "PanCK", review = "structure",
  pending_label = "Unreviewed epithelial cells",
  decisions = c(
    Tumor = "Tumor cells", Benign = "Benign epithelial cells",
    Unresolved = "Unresolved epithelial cells"
  )
)
gk_flag(all_of = "PanCK", any_of = c("CD45", "CD3e"))
gk_refinement("RF10", "FOXP3", "CD4 T cells", "CD4 T cells", "Treg cells")
gk_override("slide-07", "CD3e",
  threshold_args = list(rule = "crossing"),
  reason = "Dim staining batch; q99 rule over-conservative (review 2026-03)"
)

}
\seealso{
Other configuration:
\code{\link[=gk_config]{gk_config()}},
\code{\link[=gk_config_hash]{gk_config_hash()}},
\code{\link[=gk_hierarchy]{gk_hierarchy()}},
\code{\link[=gk_min_support]{gk_min_support()}},
\code{\link[=gk_panel]{gk_panel()}},
\code{\link[=gk_signal_policy]{gk_signal_policy()}},
\code{\link[=gk_validate_config]{gk_validate_config()}},
\code{\link[=gk_write_config]{gk_write_config()}}
}
\concept{configuration}
