% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{gk_simulate}
\alias{gk_simulate}
\title{Simulate a multiplexed tissue slide}
\usage{
gk_simulate(
  n_cells = 8000L,
  markers = NULL,
  absent = "CD21",
  shuffled = "SMA",
  rare_fraction = 0.003,
  separation = 1,
  image_id = "sim-01",
  seed = 1L
)
}
\arguments{
\item{n_cells}{Number of cells.}

\item{markers}{\code{NULL} for all twelve example markers, or a subset.}

\item{absent}{Markers whose channel is entirely zero.}

\item{shuffled}{Markers whose values are permuted across cells, destroying
any relation to cell identity.}

\item{rare_fraction}{Fraction of cells in the rare double-positive
population.}

\item{separation}{Multiplier of the positive-population signal on the log
scale; values below 1 make gating harder.}

\item{image_id}{Image identifier.}

\item{seed}{Random seed. The caller's random number state is restored.}
}
\value{
A \code{cellspec} object (see \code{\link[=gk_cellspec]{gk_cellspec()}}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Generates a synthetic slide matching \code{\link[=gk_example_config]{gk_example_config()}}, with known
ground truth, for examples, tests and method checks. The slide contains:
\itemize{
\item lineage populations (epithelial nests, CD8 and CD4 T cells including
FOXP3-positive regulatory cells, B cells, macrophages, other immune
cells, endothelial and stromal cells);
\item spatial epithelial nests (tumour-like and small benign-like ducts) and
scattered single epithelial cells;
\item epithelial-immune contact objects co-positive for PanCK and CD45;
\item a rare CD4/CD8 double-positive T-cell population;
\item nuclear state markers (FOXP3, Ki67) with nuclear and cytoplasmic
measurements, including cytoplasmic FOXP3 artefacts that fail the
localisation rule;
\item a shared per-cell brightness component;
\item an absent channel (all zeros) and a shuffled marker.
}

Intensities are raw arbitrary units rounded to two decimals, as tool
exports typically are. Ground truth is stored in the extra cell columns
\code{sim_population} and \code{sim_structure}.
}
\examples{
x <- gk_simulate(n_cells = 2000, seed = 3)
x
table(x$cells$sim_population)

}
\seealso{
Other example data:
\code{\link[=gk_example_config]{gk_example_config()}},
\code{\link[=gk_example_path]{gk_example_path()}}
}
\concept{example data}
