% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{gk_classify}
\alias{gk_classify}
\title{Classify cells with the configured ordered hierarchy}
\usage{
gk_classify(prepared, thresholds, config, correction = NULL)
}
\arguments{
\item{prepared}{A \code{\link[=gk_prepare]{gk_prepare()}} result.}

\item{thresholds}{A \code{\link[=gk_thresholds]{gk_thresholds()}} result.}

\item{config}{The configuration used to prepare the data.}

\item{correction}{\code{NULL} or a \code{\link[=gk_correct]{gk_correct()}} result.}
}
\value{
An object of class \code{gk_classification} with \code{cells}, \code{calls},
\code{disabled_rules}, the configuration and threshold hashes, and the source
hashes needed for review and export.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Evaluates flags and ordered hierarchy rules against per-image thresholds.
This is the only phenotype evaluator in gatekeepR: the review application
and batch code both call it. A rule that depends on a non-callable required
marker is disabled and recorded; no missing marker is silently treated as
positive. Structure-review rules receive their configured pending label
until a later review decision is replayed.
}
\examples{
prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
th <- gk_thresholds(prep, gk_example_config(), seed = 1L)
cl <- gk_classify(prep, th, gk_example_config())
summary(cl)

}
\seealso{
Other classification:
\code{\link[=gk_state_calls]{gk_state_calls()}},
\code{\link[=gk_structures]{gk_structures()}}
}
\concept{classification}
