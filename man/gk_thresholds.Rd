% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{gk_thresholds}
\alias{gk_thresholds}
\title{Estimate per-image marker thresholds}
\usage{
gk_thresholds(
  prepared,
  config,
  embedding = NULL,
  correction = NULL,
  seed = 5420L
)
}
\arguments{
\item{prepared}{A \code{\link[=gk_prepare]{gk_prepare()}} result.}

\item{config}{The configuration used to prepare the data.}

\item{embedding}{An optional \code{\link[=gk_embed]{gk_embed()}} result for
\code{population_crossing}.}

\item{correction}{\code{NULL} or a \code{\link[=gk_correct]{gk_correct()}} result.}

\item{seed}{Seed used when sampling embedding populations.}
}
\value{
An object of class \code{gk_thresholds}, a data frame with one row per
image and marker, the threshold result columns, and an \code{evidence}
attribute containing density and pool data for plotting.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Estimates thresholds for identity and conditional identity markers. Support
is checked before estimation, and every method records a status rather than
inventing a cut when its input is absent, underpowered or not separated.
State-marker thresholds are estimated by \code{\link[=gk_state_calls]{gk_state_calls()}} after parent
labels are available.
}
\examples{
prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
thresholds <- gk_thresholds(prep, gk_example_config(), seed = 1L)
thresholds[, c("marker", "status", "estimate")]

}
\seealso{
Other thresholds:
\code{\link[=gk_embed]{gk_embed()}},
\code{\link[=gk_threshold_methods]{gk_threshold_methods()}}
}
\concept{thresholds}
