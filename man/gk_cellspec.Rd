% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellspec.R
\name{gk_cellspec}
\alias{gk_cellspec}
\title{Build a cell table in the cellspec 1.0 format}
\usage{
gk_cellspec(
  cells,
  measurements,
  dictionary = NULL,
  images = NULL,
  channels = NULL,
  provenance = NULL
)
}
\arguments{
\item{cells}{Data frame with one row per cell and at least the columns
\code{cell_id}, \code{image_id}, \code{x} and \code{y} (centroids in µm). \code{sample_id} defaults
to \code{image_id}. Identifiers are coerced to character.}

\item{measurements}{Numeric matrix, cells × features, rows in the order of
\code{cells}. Column names are feature IDs of the form
\code{"<compartment>:<marker>:<statistic>"}, e.g. \code{"nucleus:FOXP3:mean"}.}

\item{dictionary}{Optional feature dictionary (columns \code{feature_id},
\code{kind}, \code{marker}, \code{compartment}, \code{statistic}, \code{unit}, \code{source_name}).
Derived from the column names of \code{measurements} when \code{NULL}.}

\item{images}{Optional image table (\code{image_id}, \code{sample_id},
\code{pixel_size}). Derived from \code{cells} when \code{NULL}.}

\item{channels}{Optional channel table (\code{image_id}, \code{channel_index},
\code{channel_name}, \code{marker}). Derived from the dictionary when \code{NULL}.}

\item{provenance}{Optional list describing the origin of the data.}
}
\value{
An object of class \code{cellspec}: a list with \code{spec_version},
\code{cells}, \code{measurements}, \code{dictionary}, \code{images}, \code{channels},
\code{provenance} and \code{adjacency} (\code{NULL}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Assembles a \code{cellspec} object from a cell table and a measurement matrix.
This is an interim constructor that follows the cellspec 1.0 specification;
once cellspecR is released, its readers produce the same object from tool
exports.
}
\examples{
cells <- data.frame(
  cell_id = c("c1", "c2", "c3"), image_id = "img1",
  x = c(10, 20, 30), y = c(5, 5, 5)
)
m <- cbind("cell:CD3e:mean" = c(1, 50, 3), "nucleus:FOXP3:mean" = c(0, 12, 1))
x <- gk_cellspec(cells, m)
x

}
\seealso{
Other cell tables:
\code{\link[=gk_write_cellspec]{gk_write_cellspec()}}
}
\concept{cell tables}
