% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config-hierarchy.R
\name{gk_hierarchy}
\alias{gk_hierarchy}
\title{Declare a phenotype hierarchy}
\usage{
gk_hierarchy(
  rules,
  flags = list(),
  flag_policy = list(),
  undefined_label = "Undefined",
  excluded_label = "Excluded",
  unresolved_label = "Unresolved",
  version,
  rationale = ""
)
}
\arguments{
\item{rules}{List of \code{\link[=gk_rule]{gk_rule()}} objects, in evaluation order.}

\item{flags}{Named list of \code{\link[=gk_flag]{gk_flag()}} objects.}

\item{flag_policy}{Named list or character vector giving the policy of
every flag.}

\item{undefined_label, excluded_label, unresolved_label}{Labels for cells
matching no rule, excluded by a flag, or unresolved by a flag.}

\item{version}{Version string of the hierarchy (required).}

\item{rationale}{Free text recorded with the hierarchy.}
}
\value{
An object of class \code{gk_hierarchy}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

An ordered set of \code{\link[=gk_rule]{gk_rule()}}s (first match wins), flags evaluated before
the rules with an explicit policy each, and the labels used for cells that
match no rule, that are excluded by a flag, or that are unresolved.

Flag policies:
\itemize{
\item \code{exclude}: the cell is labelled \code{excluded_label} and not analysed.
\item \code{unresolved}: the cell is labelled \code{unresolved_label}.
\item \code{flag_only}: the flag is recorded and the cell continues through the
rules.
}

The chosen policy is part of the configuration hash and applies identically
in batch code and in the review app.
}
\examples{
gk_hierarchy(
  rules = list(
    gk_rule("R010", "T cells", all_of = c("CD45", "CD3e")),
    gk_rule("R020", "Other immune cells", all_of = "CD45")
  ),
  flags = list(mixed = gk_flag(all_of = "PanCK", any_of = "CD45")),
  flag_policy = c(mixed = "unresolved"),
  version = "1.0"
)

}
\seealso{
Other configuration:
\code{\link[=gk_config]{gk_config()}},
\code{\link[=gk_config_hash]{gk_config_hash()}},
\code{\link[=gk_min_support]{gk_min_support()}},
\code{\link[=gk_panel]{gk_panel()}},
\code{\link[=gk_signal_policy]{gk_signal_policy()}},
\code{\link[=gk_validate_config]{gk_validate_config()}},
\code{\link[=gk_write_config]{gk_write_config()}},
\code{\link{hierarchy-elements}}
}
\concept{configuration}
