% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellspec.R
\name{gk_signal_policy}
\alias{gk_signal_policy}
\title{Declare which measurement represents each marker}
\usage{
gk_signal_policy(
  marker,
  compartment = "cell",
  statistic = "mean",
  fallback_compartment = NA_character_,
  min_value = 0
)
}
\arguments{
\item{marker}{Character vector of unique marker names.}

\item{compartment}{Preferred compartment per marker: \code{"cell"},
\code{"nucleus"}, \code{"cytoplasm"} or \code{"membrane"}. Recycled.}

\item{statistic}{Statistic per marker, e.g. \code{"mean"}. Recycled.}

\item{fallback_compartment}{Compartment used when the preferred value is
unavailable, or \code{NA} for none. Recycled.}

\item{min_value}{Values below this are treated as unavailable. Recycled.}
}
\value{
A data frame of class \code{gk_signal_policy} (also \code{cs_signal_policy})
with columns \code{marker}, \code{compartment}, \code{statistic},
\code{fallback_compartment} and \code{min_value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A signal policy chooses, per marker, the compartment and statistic whose
value represents the marker, with an optional fallback compartment used per
cell when the preferred value is missing or below \code{min_value}. The result
has the same structure as \code{cellspecR::cs_signal_policy()}, so either can be
used in \code{\link[=gk_config]{gk_config()}}.
}
\examples{
gk_signal_policy(
  marker = c("CD3e", "FOXP3", "PanCK"),
  compartment = c("cell", "nucleus", "cytoplasm"),
  fallback_compartment = c(NA, NA, "cell")
)

}
\seealso{
Other configuration:
\code{\link[=gk_config]{gk_config()}},
\code{\link[=gk_config_hash]{gk_config_hash()}},
\code{\link[=gk_hierarchy]{gk_hierarchy()}},
\code{\link[=gk_min_support]{gk_min_support()}},
\code{\link[=gk_panel]{gk_panel()}},
\code{\link[=gk_validate_config]{gk_validate_config()}},
\code{\link[=gk_write_config]{gk_write_config()}},
\code{\link{hierarchy-elements}}
}
\concept{configuration}
