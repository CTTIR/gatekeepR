% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config-panel.R
\name{gk_panel}
\alias{gk_panel}
\title{Declare a marker panel}
\usage{
gk_panel(
  marker,
  role,
  transform = "asinh",
  cofactor = 5,
  threshold_method = "mixture",
  threshold_args = list(),
  parents = list(),
  localization = NULL,
  optional = FALSE,
  min_support = gk_min_support()
)
}
\arguments{
\item{marker}{Character vector of unique marker names; each must appear
in the signal policy of the configuration.}

\item{role}{Role per marker (see above).}

\item{transform}{\code{"asinh"} (\code{asinh(x / cofactor)}), \code{"log1p"} or \code{"none"}.}

\item{cofactor}{Positive cofactor for \code{asinh}.}

\item{threshold_method}{Method per marker, see \code{\link[=gk_threshold_methods]{gk_threshold_methods()}}.}

\item{threshold_args}{Method arguments: either one list used for every
marker, or a list named by marker whose elements are lists.}

\item{parents}{State markers only: the parent sets (names from
\code{parent_sets} in \code{\link[=gk_config]{gk_config()}}) within which the marker is estimated and
called. Either a character vector used for every state marker, or a list
named by marker.}

\item{localization}{\code{NULL}, or a list named by marker whose elements are
lists with \code{numerator} and \code{denominator} compartments, \code{min_ratio} and
optionally \code{statistic} (default \code{"mean"}), e.g.
\code{list(FOXP3 = list(numerator = "nucleus", denominator = "cytoplasm", min_ratio = 1.5))}.}

\item{optional}{Logical per marker. A non-callable optional marker only
disables its own rules; a non-callable required marker additionally
raises a \code{gatekeepr_warning_uncallable} warning. Neither blocks an image.}

\item{min_support}{A \code{\link[=gk_min_support]{gk_min_support()}} object used for every marker, or a
list of them named by marker.}
}
\value{
An object of class \code{gk_panel}: a list with \code{markers} (a data frame
with \code{marker}, \code{role}, \code{transform}, \code{cofactor}, \code{threshold_method},
\code{optional}) and per-marker named lists \code{threshold_args}, \code{parents},
\code{localization} and \code{min_support}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A panel lists the markers used for gating, their role and how their signal
is transformed and thresholded. Arguments are vectorised over \code{marker};
scalar arguments are recycled.

Roles:
\itemize{
\item \code{identity}: used in hierarchy rules.
\item \code{conditional_identity}: used in rules only on images where it is
callable; when it is not callable, rules requiring it are disabled and it
is treated as negative where rules exclude it.
\item \code{state}: called within parent cell types (see \code{\link[=gk_state_calls]{gk_state_calls()}}).
\item \code{context}: measured and displayed, never used in rules.
}
}
\examples{
gk_panel(
  marker = c("CD45", "CD3e", "FOXP3"),
  role = c("identity", "identity", "state"),
  threshold_method = c("mixture", "mixture", "parent_crossing"),
  parents = list(FOXP3 = "T cells"),
  localization = list(
    FOXP3 = list(numerator = "nucleus", denominator = "cytoplasm", min_ratio = 1.5)
  )
)

}
\seealso{
Other configuration:
\code{\link[=gk_config]{gk_config()}},
\code{\link[=gk_config_hash]{gk_config_hash()}},
\code{\link[=gk_hierarchy]{gk_hierarchy()}},
\code{\link[=gk_min_support]{gk_min_support()}},
\code{\link[=gk_signal_policy]{gk_signal_policy()}},
\code{\link[=gk_validate_config]{gk_validate_config()}},
\code{\link[=gk_write_config]{gk_write_config()}},
\code{\link{hierarchy-elements}}
}
\concept{configuration}
