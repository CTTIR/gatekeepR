% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corrections.R
\name{gk_correct}
\alias{gk_correct}
\title{Correct prepared marker scores}
\usage{
gk_correct(
  prepared,
  method = c("none", "robust_z", "common_score_residual", "pc1_panel_residual"),
  markers = NULL,
  row_center = FALSE
)
}
\arguments{
\item{prepared}{A \code{\link[=gk_prepare]{gk_prepare()}} result.}

\item{method}{One of \code{"none"}, \code{"robust_z"}, \code{"common_score_residual"}
or \code{"pc1_panel_residual"}.}

\item{markers}{Character vector of markers to correct. \code{NULL} selects
identity and conditional identity markers.}

\item{row_center}{Whether to subtract each cell's residual mean. This
changes the meaning of per-cell values and is \code{FALSE} by default.}
}
\value{
A \code{gk_corrected} list with \code{matrix}, \code{model}, \code{config_sha256} and
\code{source_sha256}. The model has class \code{gk_correction_model} and stores all
fitted centres, scales, rotations and coefficients.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Applies a deterministic correction to transformed marker scores. Markers
that fail the raw support checks are excluded from fitting and are \code{NA} in
the corrected matrix, so correction cannot create signal in an absent
channel. The fitted parameters are returned in a
\code{gk_correction_model} and can be applied again to the same prepared data.
}
\examples{
prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
corrected <- gk_correct(prep, method = "robust_z")
corrected$model

}
\seealso{
Other corrections:
\code{\link[=gk_fit_residual_model]{gk_fit_residual_model()}}
}
\concept{corrections}
