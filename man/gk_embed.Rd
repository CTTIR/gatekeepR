% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{gk_embed}
\alias{gk_embed}
\title{Compute a reproducible marker embedding and density populations}
\usage{
gk_embed(
  prepared,
  correction = NULL,
  n_pcs = 10L,
  n_neighbors = 20L,
  min_dist = 0.15,
  grid = 400L,
  bg = 0.03,
  tolerance = 0.03,
  basin_method = c("hillclimb", "ebimage"),
  small_basins = c("uncertain", "merge"),
  min_abs = 30L,
  min_frac = 0.002,
  seed = 42L
)
}
\arguments{
\item{prepared}{A \code{\link[=gk_prepare]{gk_prepare()}} result.}

\item{correction}{\code{NULL} or a \code{\link[=gk_correct]{gk_correct()}} result.}

\item{n_pcs}{Number of principal components to retain.}

\item{n_neighbors}{UMAP neighbourhood size.}

\item{min_dist}{UMAP minimum distance.}

\item{grid}{Number of KDE grid points in each dimension.}

\item{bg}{Relative density below which KDE cells are background.}

\item{tolerance}{Basin merge or watershed tolerance.}

\item{basin_method}{\code{"hillclimb"} or optional \code{"ebimage"}.}

\item{small_basins}{Whether small basins become uncertain or merge.}

\item{min_abs}{Minimum cells for a retained basin.}

\item{min_frac}{Minimum fraction for a retained basin.}

\item{seed}{Random seed used by UMAP.}
}
\value{
An object of class \code{gk_embedding} with \code{pca}, \code{umap},
\code{population}, KDE data, basin information, parameters and provenance
hashes.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Computes a sign-canonicalised PCA, a deterministic two-dimensional UMAP,
and density basins used by \code{population_crossing} thresholds. The default
basin implementation is pure R. \code{small_basins = "uncertain"} retains cells
while assigning their population as \code{NA}; \code{"merge"} assigns them to the
nearest retained basin.
}
\examples{
if (requireNamespace("uwot", quietly = TRUE)) {
  prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
  emb <- gk_embed(prep, n_pcs = 3L, grid = 40L, seed = 7L)
  emb
}

}
\seealso{
Other thresholds:
\code{\link[=gk_threshold_methods]{gk_threshold_methods()}},
\code{\link[=gk_thresholds]{gk_thresholds()}}
}
\concept{thresholds}
