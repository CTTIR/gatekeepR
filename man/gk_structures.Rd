% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structures.R
\name{gk_structures}
\alias{gk_structures}
\title{Find epithelial structures for review}
\usage{
gk_structures(
  classification,
  prepared,
  eps_min = 15,
  eps_max = 30,
  min_pts = 10L,
  min_cells = 20L
)
}
\arguments{
\item{classification}{A \code{\link[=gk_classify]{gk_classify()}} result.}

\item{prepared}{The matching \code{\link[=gk_prepare]{gk_prepare()}} result.}

\item{eps_min, eps_max}{Lower and upper bounds for DBSCAN epsilon in the
coordinate units of the cellspec.}

\item{min_pts}{DBSCAN minimum points.}

\item{min_cells}{Minimum cells for an exported structure.}
}
\value{
An object of class \code{gk_structures} with \code{structures}, \code{cell_structure},
parameters and provenance hashes.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Runs deterministic centroid-based DBSCAN on cells awaiting a configured
structure decision. Epsilon is the 95th percentile of the \code{(min_pts - 1)}th
nearest-neighbour distance, bounded by \code{eps_min} and \code{eps_max}. Retained
structures are ordered by decreasing size and then centroid coordinates,
making IDs stable under input row permutation.
}
\examples{
prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
th <- gk_thresholds(prep, gk_example_config(), seed = 1L)
cl <- gk_classify(prep, th, gk_example_config())
if (requireNamespace("dbscan", quietly = TRUE)) gk_structures(cl, prep)

}
\seealso{
Other classification:
\code{\link[=gk_classify]{gk_classify()}},
\code{\link[=gk_state_calls]{gk_state_calls()}}
}
\concept{classification}
