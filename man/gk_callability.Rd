% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callability.R
\name{gk_callability}
\alias{gk_callability}
\title{Callability of markers per image}
\usage{
gk_callability(x, ...)
}
\arguments{
\item{x}{A \code{gk_prepared} object, or a later result carrying callability
(thresholds, classification, review).}

\item{...}{Unused.}
}
\value{
A data frame with \code{image_id}, \code{marker}, \code{role}, \code{status},
\code{details} and support statistics (\code{n_cells}, \code{n_available},
\code{fraction_unavailable}, \code{sd}, \code{mad}, \code{fraction_mode}, \code{q01}, \code{q50},
\code{q99}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reports, for every image and marker, whether the marker can be called and
why not. Checks on the raw selected signal run in \code{\link[=gk_prepare]{gk_prepare()}} before any
correction or threshold; threshold estimation adds its own outcomes.\tabular{ll}{
   Status \tab Meaning \cr
   \code{NOT_CALLABLE_ABSENT} \tab constant or near-constant signal \cr
   \code{NOT_CALLABLE_UNAVAILABLE} \tab too many cells without usable signal \cr
   \code{NOT_CALLABLE_UNDERPOWERED} \tab too few cells, or too few in a pool \cr
   \code{NOT_CALLABLE_NO_POPULATION} \tab the method finds no positive population \cr
   \code{NOT_CALLABLE_NO_SEPARATION} \tab positive and negative are not separated \cr
   \code{PENDING_THRESHOLD} \tab supported; threshold not yet estimated \cr
   \code{NOT_GATED} \tab context marker with usable signal \cr
   \code{CALLABLE} \tab finite threshold, all checks passed \cr
   \code{MANUAL} \tab cut set by a reviewer or the configuration \cr
}


A marker that is not callable never produces positive calls, and rules
requiring it are disabled and listed.
}
\examples{
prep <- gk_prepare(gk_example_path("example-slide"), gk_example_config(), quiet = TRUE)
cb <- gk_callability(prep)
cb[cb$status != "PENDING_THRESHOLD", c("image_id", "marker", "status", "details")]

}
\seealso{
Other preparation:
\code{\link[=gk_prepare]{gk_prepare()}}
}
\concept{preparation}
