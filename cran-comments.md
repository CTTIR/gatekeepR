## Version 1.0.0

## Test environments

* Ubuntu 26.04, R 4.6.1.
* `R CMD check --as-cran`.

## R CMD check results

There were no errors or warnings. The local `--as-cran` run reports only the
expected new-submission note and a runtime note for the optional UMAP example.

The package contains only simulated and study-neutral example data.
