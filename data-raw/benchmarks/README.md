# Benchmarks

This directory contains opt-in benchmark templates. Benchmark inputs and
outputs are intentionally excluded from the package build and must not contain
private or identifying image data.

Run `performance.R` from the repository root when comparing versions. Record
the R version, package versions, configuration hash, cell count, and elapsed
time with the result.
